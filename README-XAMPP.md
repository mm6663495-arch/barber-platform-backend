# دليل تشغيل منصة صالونات الحلاقة على XAMPP

## 🚀 التشغيل السريع

### الطريقة الأولى: استخدام السكريبت التلقائي

#### Windows (PowerShell):
```powershell
# تشغيل السكريبت
.\setup-xampp.ps1
```

#### Linux/Mac (Bash):
```bash
# إعطاء صلاحيات التنفيذ
chmod +x setup-xampp.sh

# تشغيل السكريبت
./setup-xampp.sh
```

### الطريقة الثانية: التشغيل اليدوي

#### 1. تشغيل XAMPP
- شغل **Apache** و **MySQL** من XAMPP Control Panel

#### 2. إنشاء قاعدة البيانات
```sql
-- في phpMyAdmin أو MySQL CLI
CREATE DATABASE barber_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### 3. إعداد البيئة
```bash
# نسخ ملف التكوين
cp xampp-config.env .env
```

#### 4. تثبيت التبعيات
```bash
npm install
```

#### 5. إعداد قاعدة البيانات
```bash
# توليد Prisma Client
npx prisma generate

# تشغيل Migrations
npx prisma migrate dev --name init

# إضافة البيانات التجريبية
npm run db:seed
```

#### 6. تشغيل التطبيق
```bash
npm run start:dev
```

## 📋 الحسابات التجريبية

بعد التشغيل، ستجد الحسابات التالية:

### 👑 مدير النظام
- **البريد الإلكتروني**: `admin@barber.com`
- **كلمة المرور**: `admin123`
- **الصلاحيات**: إدارة كاملة للمنصة

### ✂️ صاحب الصالون
- **البريد الإلكتروني**: `owner@salon.com`
- **كلمة المرور**: `owner123`
- **الصلاحيات**: إدارة الصالون والباقات

### 👤 العميل
- **البريد الإلكتروني**: `customer@test.com`
- **كلمة المرور**: `customer123`
- **الصلاحيات**: استخدام الخدمات

## 🔗 الروابط المهمة

- **API الرئيسي**: http://localhost:3000
- **وثائق API**: http://localhost:3000/api/docs
- **phpMyAdmin**: http://localhost/phpmyadmin
- **Prisma Studio**: `npm run db:studio`

## 🛠️ الأوامر المفيدة

```bash
# إعداد كامل للمشروع
npm run setup:xampp

# تشغيل مع إعداد تلقائي
npm run start:xampp

# إعادة تعيين قاعدة البيانات
npm run db:reset

# إضافة البيانات التجريبية
npm run db:seed

# فتح Prisma Studio
npm run db:studio

# تشغيل Migrations جديدة
npm run db:migrate

# توليد Prisma Client
npm run db:generate
```

## 🔧 حل المشاكل الشائعة

### مشكلة: "Connection refused"
```bash
# تأكد من تشغيل MySQL في XAMPP
# تحقق من المنفذ في XAMPP Control Panel (عادة 3306)
```

### مشكلة: "Database does not exist"
```sql
-- في phpMyAdmin
CREATE DATABASE barber_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### مشكلة: "Permission denied"
```bash
# إنشاء مجلد uploads
mkdir uploads
chmod 755 uploads
```

### مشكلة: "Module not found"
```bash
# إعادة تثبيت التبعيات
rm -rf node_modules package-lock.json
npm install
```

## 📊 هيكل قاعدة البيانات

المشروع يتضمن الجداول التالية:
- **Users**: المستخدمون (مدير، صاحب صالون، عميل)
- **Salons**: الصالونات
- **Packages**: الباقات والخدمات
- **Subscriptions**: الاشتراكات
- **Visits**: الزيارات
- **Reviews**: التقييمات
- **Payments**: المدفوعات
- **Notifications**: الإشعارات
- **Reports**: البلاغات
- **AuditLog**: سجل الأنشطة

## 🔐 إعدادات الأمان

- **JWT**: للمصادقة والتفويض
- **Bcrypt**: لتشفير كلمات المرور
- **Rate Limiting**: حماية من الهجمات
- **CORS**: حماية من الطلبات غير المصرح بها
- **Validation**: تحقق من صحة البيانات

## 📱 اختبار API

### تسجيل الدخول
```bash
curl -X POST http://localhost:3000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@barber.com",
    "password": "admin123"
  }'
```

### الحصول على الصالونات
```bash
curl -X GET http://localhost:3000/salons
```

### الحصول على الباقات
```bash
curl -X GET http://localhost:3000/salons/1/packages
```

## 🚀 النشر على الإنتاج

للنشر على خادم إنتاج:
1. تحديث متغيرات البيئة
2. استخدام قاعدة بيانات إنتاج
3. تشغيل `npm run build`
4. تشغيل `npm run start:prod`

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من تشغيل XAMPP
2. تأكد من إعدادات قاعدة البيانات
3. راجع ملف `.env`
4. تحقق من console للأخطاء