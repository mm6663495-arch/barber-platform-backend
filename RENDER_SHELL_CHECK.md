# 🔍 استخدام Render Shell للتحقق من المشكلة

## 📋 خطوات التحقق

### في Render Dashboard:

1. **Web Service → Shell**
2. **نفّذ هذه الأوامر:**

```bash
# 1. التحقق من الموقع الحالي
pwd

# 2. البحث عن main.js في أي مكان
find . -name "main.js" -type f

# 3. التحقق من وجود dist/
ls -la

# 4. إذا وجد dist/، تحقق من محتوياته
ls -la dist/

# 5. البحث عن dist/ في أي مكان
find . -type d -name "dist"

# 6. التحقق من محتويات src/
ls -la src/
```

---

## 🎯 بعد الحصول على النتائج

**شارك نتائج الأوامر أعلاه** وسأحدد المشكلة بالضبط:

- إذا كان `main.js` موجود في مكان مختلف
- إذا كان `dist/` غير موجود
- إذا كان المسار مختلف

---

**🔍 بعد تشغيل هذه الأوامر في Render Shell، سنعرف المشكلة بالضبط!**

