# 🤖 دليل استخدام بوت الاختبار التلقائي

## نظرة عامة

هذا البوت يقوم بـ **اختبار جميع APIs تلقائياً** بدون أي تدخل يدوي!

---

## 🚀 كيفية التشغيل

### الخطوة 1: تشغيل المشروع
```bash
npm run start:dev
```

### الخطوة 2: تشغيل البوت (في terminal جديد)
```bash
node test-api-bot.js
```

---

## ✨ ماذا يفعل البوت؟

البوت يختبر **9 مراحل** تلقائياً:

### 1️⃣ المصادقة (Authentication)
- ✅ تسجيل دخول Admin
- ✅ تسجيل دخول Salon Owner
- ✅ تسجيل دخول Customer
- ✅ عرض الملف الشخصي

### 2️⃣ المستخدمين (Users)
- ✅ عرض جميع المستخدمين
- ✅ إحصائيات المستخدمين
- ✅ المستخدمون الجدد
- ✅ عرض مستخدم محدد

### 3️⃣ الصالونات (Salons)
- ✅ عرض جميع الصالونات (عام)
- ✅ الصالونات الشائعة
- ✅ صالوناتي (Salon Owner)
- ✅ تفاصيل صالون
- ✅ باقات الصالون
- ✅ إحصائيات صالوناتي

### 4️⃣ الباقات (Packages)
- ✅ إنشاء باقة جديدة
- ✅ نشر الباقة
- ✅ عرض تفاصيل الباقة

### 5️⃣ الاشتراكات (Subscriptions)
- ✅ إنشاء اشتراك جديد
- ✅ عرض اشتراكاتي
- ✅ تفاصيل الاشتراك
- ✅ البحث بـ QR Code
- ✅ إحصائيات الاشتراكات

### 6️⃣ المدفوعات (Payments)
- ✅ عرض مدفوعاتي
- ✅ إحصائيات المدفوعات
- ✅ المدفوعات الأخيرة

### 7️⃣ المراجعات (Reviews)
- ✅ عرض جميع المراجعات
- ✅ المراجعات الأخيرة
- ✅ إحصائيات المراجعات
- ✅ مراجعات صالون محدد

### 8️⃣ الإشعارات (Notifications)
- ✅ عرض إشعاراتي
- ✅ عدد الإشعارات غير المقروءة

### 9️⃣ لوحة الإدارة (Admin)
- ✅ لوحة التحكم
- ✅ النشاط الأخير
- ✅ صحة النظام
- ✅ تحليلات الإيرادات

---

## 📊 ماذا ستحصل؟

عند تشغيل البوت، سترى:

```
🤖 بدء اختبار API تلقائياً...

=== المرحلة 1: اختبار المصادقة ===
✓ نجح: تسجيل دخول Admin (200)
✓ تم حفظ Admin Token
✓ نجح: تسجيل دخول Salon Owner (200)
...

=== النتيجة النهائية ===
إجمالي الاختبارات: 45
نجح: 43
فشل: 2
الوقت المستغرق: 52.3 ثانية

معدل النجاح: 95.6%

🎉 رائع! النظام يعمل بشكل ممتاز!
```

---

## ⚙️ التخصيص

يمكنك تعديل إعدادات البوت في بداية ملف `test-api-bot.js`:

```javascript
const BASE_URL = 'http://localhost:3000';  // عنوان الخادم
const DELAY = 1000;                        // التأخير بين الطلبات (ميلي ثانية)
```

---

## 🎯 الميزات

### ✅ اختبار تلقائي كامل
- لا يحتاج أي تدخل يدوي
- يختبر جميع الحالات تلقائياً

### ✅ تقرير تفصيلي
- عدد الاختبارات الناجحة والفاشلة
- قائمة بجميع الأخطاء
- معدل النجاح النهائي

### ✅ ألوان واضحة
- ✅ أخضر للنجاح
- ❌ أحمر للفشل
- ℹ️ أزرق للمعلومات
- ⚠️ أصفر للتحذيرات

### ✅ حفظ البيانات تلقائياً
- يحفظ Tokens تلقائياً
- يحفظ IDs للاستخدام في الاختبارات التالية
- يربط الاختبارات ببعضها

---

## 🔧 متطلبات التشغيل

1. تأكد أن المشروع يعمل على `http://localhost:3000`
2. تأكد أن قاعدة البيانات تحتوي على بيانات Seed:
   ```bash
   npm run db:seed
   ```

---

## 📝 ملاحظات

- البوت ينتظر 1 ثانية بين كل طلب لتجنب الحظر (Rate Limiting)
- إذا فشل اختبار، سيستمر البوت في الاختبارات الأخرى
- البوت يعرض timestamp لكل اختبار
- يمكنك تشغيل البوت متى شئت للتحقق من صحة النظام

---

## 🆘 حل المشاكل

### المشكلة: "connect ECONNREFUSED"
**الحل:** تأكد أن المشروع يعمل على `http://localhost:3000`

### المشكلة: "Invalid token"
**الحل:** تأكد من تشغيل `npm run db:seed` أولاً

### المشكلة: البوت بطيء
**الحل:** قلل قيمة `DELAY` في بداية الملف

---

## 🎉 جاهز!

الآن يمكنك اختبار جميع APIs تلقائياً بأمر واحد فقط:

```bash
node test-api-bot.js
```

**استمتع بالاختبار التلقائي! 🚀**

