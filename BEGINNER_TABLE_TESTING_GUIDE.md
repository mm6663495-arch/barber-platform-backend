# دليل فحص الجداول للمبتدئين - خطوة بخطوة 📚

هذا دليل تفصيلي للمبتدئين لفحص جميع جداول منصة الحلاقة بالترتيب الصحيح.

## 🎯 الهدف من هذا الدليل
تعلم كيفية فحص واختبار كل جدول في قاعدة البيانات من خلال Swagger API بطريقة مبسطة ومنظمة.

## 📋 المتطلبات الأساسية
- تشغيل المشروع: `npm run start:xampp`
- فتح المتصفح على: `http://localhost:3000/api/docs`
- فهم أساسي لاستخدام المتصفح

---

## 🔧 التحضير الأولي

### الخطوة 1: تشغيل المشروع
```bash
# في terminal/command prompt
cd d:\barber-platform-backend
npm run start:xampp
```

### الخطوة 2: فتح Swagger UI
1. افتح المتصفح
2. اذهب إلى: `http://localhost:3000/api/docs`
3. ستظهر صفحة Swagger UI

### الخطوة 3: الحصول على بيانات تسجيل الدخول
سنستخدم حساب المدير للوصول لجميع الجداول:
- **Email:** `admin@barber.com`
- **Password:** `admin123`



---

## 📊 فحص الجداول بالترتيب

### المرحلة الأولى: الجداول العامة (بدون مصادقة)

#### الجدول 1: الصالونات (Salons)
**الهدف:** عرض جميع الصالونات المتاحة

**الخطوات:**
1. في Swagger UI، ابحث عن قسم **Salons**
2. اضغط على `GET /salons` ليفتح
3. اضغط **Try it out**
4. اضغط **Execute**
5. **النتيجة المتوقعة:** قائمة بالصالونات مع معلوماتها

**ما تبحث عنه:**
- معرف الصالون (id)
- اسم الصالون (name)
- العنوان (address)
- التقييم (rating)

---

### المرحلة الثانية: الحصول على المصادقة

#### الخطوة 4: تسجيل الدخول
1. ابحث عن قسم **Authentication**
2. اضغط على `POST /auth/login`
3. اضغط **Try it out**
4. في الـ Request body، أدخل:
```json
{
  "email": "admin@barber.com",
  "password": "admin123"
}
```
5. اضغط **Execute**
6. **انسخ الـ accessToken** من الاستجابة

#### الخطوة 5: تفعيل المصادقة
1. اضغط زر **Authorize** في أعلى الصفحة
2. في حقل "Value"، أدخل: `Bearer YOUR_TOKEN_HERE`
3. اضغط **Authorize**
4. اضغط **Close**

---

### المرحلة الثالثة: جداول المستخدمين

#### الجدول 2: المستخدمون (Users)
**الهدف:** عرض جميع المستخدمين في النظام

**الخطوات:**
1. ابحث عن قسم **Users**
2. اضغط على `GET /users`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف المستخدم (id)
- البريد الإلكتروني (email)
- الدور (role): ADMIN, SALON_OWNER, CUSTOMER
- حالة النشاط (isActive)

#### الجدول 3: إحصائيات المستخدمين
1. اضغط على `GET /users/statistics`
2. اضغط **Try it out**
3. اضغط **Execute**

**ما تبحث عنه:**
- عدد المستخدمين الإجمالي
- عدد كل نوع من المستخدمين
- المستخدمون النشطون

#### الجدول 4: المستخدمون الجدد
1. اضغط على `GET /users/recent`
2. اضغط **Try it out**
3. اضغط **Execute**

---

### المرحلة الرابعة: جداول الصالونات المفصلة

#### الجدول 5: تفاصيل صالون محدد
1. ابحث عن قسم **Salons**
2. اضغط على `GET /salons/{id}`
3. اضغط **Try it out**
4. أدخل معرف صالون (مثل: 1)
5. اضغط **Execute**

#### الجدول 6: باقات الصالون
1. اضغط على `GET /salons/{id}/packages`
2. اضغط **Try it out**
3. أدخل معرف الصالون (مثل: 1)
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف الباقة (id)
- اسم الباقة (name)
- السعر (price)
- عدد الزيارات (visitsCount)

---

### المرحلة الخامسة: جداول الاشتراكات

#### الجدول 7: جميع الاشتراكات
1. ابحث عن قسم **Subscriptions**
2. اضغط على `GET /subscriptions`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف الاشتراك (id)
- معرف العميل (customerId)
- معرف الباقة (packageId)
- رمز QR (qrCode)
- الحالة (status): ACTIVE, EXPIRED, CANCELLED

#### الجدول 8: إحصائيات الاشتراكات
1. اضغط على `GET /subscriptions/statistics`
2. اضغط **Try it out**
3. اضغط **Execute**

---

### المرحلة السادسة: جداول المراجعات

#### الجدول 9: جميع المراجعات
1. ابحث عن قسم **Reviews**
2. اضغط على `GET /reviews`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف المراجعة (id)
- معرف العميل (customerId)
- معرف الصالون (salonId)
- التقييم (rating): 1-5
- التعليق (comment)

#### الجدول 10: مراجعات صالون محدد
1. اضغط على `GET /reviews/salon/{salonId}`
2. اضغط **Try it out**
3. أدخل معرف الصالون (مثل: 1)
4. اضغط **Execute**

#### الجدول 11: إحصائيات المراجعات
1. اضغط على `GET /reviews/statistics`
2. اضغط **Try it out**
3. اضغط **Execute**

---

### المرحلة السابعة: جداول المدفوعات

#### الجدول 12: جميع المدفوعات
1. ابحث عن قسم **Payments**
2. اضغط على `GET /payments`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف الدفع (id)
- معرف الاشتراك (subscriptionId)
- المبلغ (amount)
- الحالة (status): PENDING, COMPLETED, FAILED, REFUNDED

#### الجدول 13: إحصائيات المدفوعات
1. اضغط على `GET /payments/statistics`
2. اضغط **Try it out**
3. اضغط **Execute**

#### الجدول 14: المدفوعات الأخيرة
1. اضغط على `GET /payments/recent`
2. اضغط **Try it out**
3. اضغط **Execute**

---

### المرحلة الثامنة: جداول الإشعارات

#### الجدول 15: جميع الإشعارات
1. ابحث عن قسم **Notifications**
2. اضغط على `GET /notifications`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- معرف الإشعار (id)
- معرف المستخدم (userId)
- العنوان (title)
- الرسالة (message)
- النوع (type): SUBSCRIPTION, VISIT, REVIEW, etc.

---

### المرحلة التاسعة: جداول لوحة الإدارة

#### الجدول 16: إحصائيات عامة
1. ابحث عن قسم **Admin**
2. اضغط على `GET /admin/statistics`
3. اضغط **Try it out**
4. اضغط **Execute**

**ما تبحث عنه:**
- إحصائيات المستخدمين
- إحصائيات الصالونات
- إحصائيات المدفوعات
- إحصائيات المراجعات

---

## ✅ قائمة التحقق النهائية

### تأكد من فحص كل جدول:
- [ ] الصالونات العامة
- [ ] المستخدمون
- [ ] إحصائيات المستخدمين
- [ ] تفاصيل الصالونات
- [ ] باقات الصالونات
- [ ] الاشتراكات
- [ ] إحصائيات الاشتراكات
- [ ] المراجعات
- [ ] مراجعات الصالونات
- [ ] إحصائيات المراجعات
- [ ] المدفوعات
- [ ] إحصائيات المدفوعات
- [ ] المدفوعات الأخيرة
- [ ] الإشعارات
- [ ] إحصائيات الإدارة

---

## 🔍 نصائح للمبتدئين

### 1. فهم الاستجابات
- **200 OK:** نجح الطلب
- **401 Unauthorized:** تحتاج تسجيل دخول
- **403 Forbidden:** لا تملك الصلاحيات
- **404 Not Found:** المورد غير موجود

### 2. قراءة البيانات
- **id:** معرف فريد للسجل
- **createdAt:** تاريخ الإنشاء
- **updatedAt:** تاريخ آخر تحديث
- **isActive:** هل السجل نشط أم لا

### 3. فهم العلاقات
- **customerId:** يشير إلى جدول العملاء
- **salonId:** يشير إلى جدول الصالونات
- **subscriptionId:** يشير إلى جدول الاشتراكات

### 4. استخدام الفلاتر
- **page:** رقم الصفحة
- **limit:** عدد النتائج في الصفحة
- **status:** فلترة حسب الحالة

---

## 🆘 حل المشاكل الشائعة

### مشكلة: خطأ 401 Unauthorized
**الحل:**
1. تأكد من تسجيل الدخول أولاً
2. انسخ الـ token بشكل صحيح
3. تأكد من إضافة "Bearer " قبل الـ token

### مشكلة: خطأ 403 Forbidden
**الحل:**
1. تأكد من استخدام حساب المدير
2. تأكد من تفعيل المصادقة بشكل صحيح

### مشكلة: لا تظهر بيانات
**الحل:**
1. تأكد من تشغيل `npm run db:seed`
2. تأكد من تشغيل قاعدة البيانات
3. جرب إعادة تشغيل المشروع

### مشكلة: صفحة Swagger لا تفتح
**الحل:**
1. تأكد من تشغيل المشروع
2. تأكد من الرابط: `http://localhost:3000/api/docs`
3. تأكد من عدم وجود تطبيق آخر يستخدم المنفذ 3000

---

## 🎉 مبروك!

بعد اتباع هذا الدليل، ستكون قد:
- ✅ فحصت جميع الجداول الرئيسية
- ✅ فهمت كيفية استخدام Swagger API
- ✅ تعلمت كيفية قراءة البيانات
- ✅ أصبحت جاهزاً للاختبارات المتقدمة

**الخطوة التالية:** راجع `API_TESTING_GUIDE.md` للتعلم عن إنشاء وتعديل البيانات!

---

**💡 تذكر: لا تتردد في تكرار أي خطوة إذا لم تفهمها. التعلم يأتي بالممارسة!**
