# دليل البدء السريع - اختبار API

## 🚀 البدء في 5 دقائق

### 1. تشغيل المشروع
```bash
# تثبيت التبعيات
npm install

# تشغيل قاعدة البيانات والتطبيق
npm run start:xampp
```

### 2. فتح Swagger UI
اذهب إلى: **http://localhost:3000/api/docs**

### 3. تسجيل الدخول والحصول على Token

#### للاختبار كمدير:
```json
POST /auth/login
{
  "email": "admin@barber.com",
  "password": "admin123"
}
```

#### للاختبار كصاحب صالون:
```json
POST /auth/login
{
  "email": "owner@salon.com", 
  "password": "owner123"
}
```

#### للاختبار كعميل:
```json
POST /auth/login
{
  "email": "customer@test.com",
  "password": "customer123"
}
```

### 4. تفعيل المصادقة
1. اضغط **Authorize** في Swagger
2. أدخل: `Bearer YOUR_TOKEN_HERE`
3. اضغط **Authorize**

## 🧪 اختبارات سريعة

### عرض الصالونات (بدون مصادقة)
```
GET /salons
```

### إنشاء صالون جديد (صاحب صالون)
```
POST /salons
{
  "name": "صالون تجريبي",
  "address": "الرياض، المملكة العربية السعودية",
  "latitude": 24.7136,
  "longitude": 46.6753,
  "description": "صالون تجريبي للاختبار"
}
```

### إنشاء باقة جديدة
```
POST /salons/1/packages
{
  "name": "باقة تجريبية",
  "price": 100.0,
  "visitsCount": 3,
  "validityDays": 30,
  "description": "باقة تجريبية للاختبار"
}
```

### إنشاء اشتراك (عميل)
```
POST /subscriptions
{
  "packageId": 1,
  "paymentMethod": "cash"
}
```

### كتابة مراجعة (عميل)
```
POST /reviews
{
  "salonId": 1,
  "visitId": 1,
  "rating": 5,
  "comment": "خدمة ممتازة!"
}
```

## 📊 الإحصائيات السريعة

### إحصائيات المستخدمين (مدير)
```
GET /users/statistics
```

### إحصائيات الصالونات (صاحب صالون)
```
GET /salons/owner/statistics
```

### إحصائيات المدفوعات
```
GET /payments/statistics
```

## 🔍 نصائح سريعة

1. **ابدأ بالعمليات العامة** (عرض الصالونات)
2. **احصل على token** قبل العمليات المحمية
3. **استخدم البيانات التجريبية** الموجودة
4. **اختبر الأدوار المختلفة** (Admin/Owner/Customer)
5. **تحقق من الاستجابات** للتأكد من صحة البيانات

## 🆘 حل المشاكل السريع

### خطأ 401: تأكد من وجود Token
### خطأ 403: تأكد من الصلاحيات  
### خطأ 400: تحقق من البيانات المرسلة
### خطأ 404: تأكد من وجود المورد

---
**🎯 الهدف: اختبار جميع الوظائف في أقل وقت ممكن!**
